// Tipos de dados
const nome = 'Joao'; // srting
const nome1 = "Joao"; // srting
const nome2 = `Joao`; // srting
const num1 = 10; // number
const num2 = 10.50; // number
const nomeAluno = undefined; // undefined  -> não aponta para local nenhum na memória
const sobrenomeAluno = null; // null não aponta para local nenhum na memória
const aprovado = undefined; // boolean

// Operadores aritméticos
/**
 * + adição / concatenação
 * - subtrair
 * / divisão
 * * multiplicação
 * ++ incrementar
 * -- decrementar
 * += somar e alocar
 * -= subtrair e alocar
 * ** elevar
 */



